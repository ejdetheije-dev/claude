# Claude Memory

## User: ejdetheije-dev

### GitHub Commit Origins
When asked about GitHub commits, always check **all repos** on the `ejdetheije-dev` account via the GitHub API.

Distinguish between two environments:
- **Laptop (Cursor + Claude Code)** — commits made locally on the user's laptop using Claude Code integrated in Cursor (e.g., `kanban` repo)
- **Web/Cloud (Claude Agent SDK)** — commits made via this web-based Claude environment (e.g., `claude` repo)

Always label commits by their origin in responses.
